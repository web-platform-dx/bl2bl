{
  "name": "bl2bl",
  "version": "0.1.7",
  "description": "A tool that turns Web Platform Baseline targets into browserslist configurations.",
  "main": "get-baseline-versions.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npx prettier . --write"
  },
  "files": [
    "bin",
    "LICENSE.txt",
    "README.md"
  ],
  "author": "",
  "license": "Apache-2.0",
  "bin": "./bin/cli.js",
  "dependencies": {
    "@mdn/browser-compat-data": "latest",
    "baseline-browser-mapping": "latest"
  },
  "devDependencies": {
    "prettier": "3.4.2"
  }
}
